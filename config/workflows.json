{
  "schemaVersion": 1,
  "description": "Workflow registry for stable script and future UI entry points. Paths are repository-relative and intentionally contain no machine-specific values.",
  "workflows": [
    {
      "id": "profile-local-hardware",
      "name": "Profile Local Hardware",
      "purpose": "Collect sanitized CPU, RAM, GPU, VRAM, platform, and local model information for model recommendation.",
      "category": "discovery",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["outputPath", "ollamaBaseUrl"],
      "outputs": ["modelProfileJson", "modelProfileMarkdown"],
      "entryPoints": {
        "windows": "scripts/get-local-model-profile.windows.ps1",
        "linux": "scripts/get-local-model-profile.linux.sh",
        "macos": "scripts/get-local-model-profile.macos.sh"
      }
    },
    {
      "id": "profile-remote-hardware",
      "name": "Profile Remote Hardware",
      "purpose": "Collect sanitized hardware and model profile information from a remote model host over SSH.",
      "category": "discovery",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["sshTarget", "outputPath", "allowInteractiveSsh"],
      "outputs": ["remoteModelProfileJson"],
      "entryPoints": {
        "windows": "scripts/get-remote-model-profile.ps1",
        "linux": "scripts/get-remote-model-profile.linux.sh",
        "macos": "scripts/get-remote-model-profile.macos.sh"
      }
    },
    {
      "id": "discover-online-models",
      "name": "Discover Online Model Candidates",
      "purpose": "Suggest candidate local models from public model metadata while keeping recommendations evidence-gated.",
      "category": "model-selection",
      "safetyLevel": "network-read",
      "uiReady": true,
      "inputs": ["sources", "queries", "families", "modelProfilePath", "vramSelectionMode", "outputPath"],
      "outputs": ["onlineModelCandidatesJson"],
      "entryPoints": {
        "windows": "scripts/discover-online-model-candidates.ps1",
        "linux": "scripts/discover-online-model-candidates.linux.sh",
        "macos": "scripts/discover-online-model-candidates.macos.sh"
      }
    },
    {
      "id": "build-model-catalog",
      "name": "Build Security-Aware Model Catalog",
      "purpose": "Combine candidate metadata, exact artifact identity, license policy, hardware fit, and validation evidence without downloading or promoting models.",
      "category": "model-selection",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["discoveryReportPath", "outputPath"],
      "outputs": ["modelCatalogJson"],
      "entryPoints": {
        "windows": "scripts/build-model-catalog.ps1",
        "linux": "scripts/build-model-catalog.linux.sh",
        "macos": "scripts/build-model-catalog.macos.sh"
      }
    },
    {
      "id": "generate-model-scorecard",
      "name": "Generate Model Scorecard",
      "purpose": "Summarize model readiness by model, surface, status, and evidence from the committed evidence catalog.",
      "category": "model-selection",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["evidenceCatalogPath", "modelCatalogPath", "outputPath", "markdownOutputPath"],
      "outputs": ["modelScorecardJson", "modelScorecardMarkdown"],
      "entryPoints": {
        "windows": "scripts/generate-model-scorecard.ps1",
        "linux": "scripts/generate-model-scorecard.linux.sh",
        "macos": "scripts/generate-model-scorecard.macos.sh"
      }
    },
    {
      "id": "generate-evidence-dashboard",
      "name": "Generate Evidence Dashboard",
      "purpose": "Summarize evidence status, agent surface readiness, and tested model coverage from committed sanitized catalogs.",
      "category": "evidence",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["evidenceCatalogPath", "surfaceMatrixPath", "surfaceSolutionPath", "outputPath", "markdownOutputPath"],
      "outputs": ["evidenceDashboardJson", "evidenceDashboardMarkdown"],
      "entryPoints": {
        "windows": "scripts/generate-evidence-dashboard.ps1",
        "linux": "scripts/generate-evidence-dashboard.linux.sh",
        "macos": "scripts/generate-evidence-dashboard.macos.sh"
      }
    },
    {
      "id": "get-beginner-setup-plan",
      "name": "Get Beginner Setup Plan",
      "purpose": "Create an ordered first-run setup plan that links beginner commands to stable workflow registry entries.",
      "category": "onboarding",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["platform", "outputPath", "markdownOutputPath"],
      "outputs": ["beginnerSetupPlanJson", "beginnerSetupPlanMarkdown"],
      "entryPoints": {
        "windows": "scripts/get-beginner-setup-plan.ps1",
        "linux": "scripts/get-beginner-setup-plan.linux.sh",
        "macos": "scripts/get-beginner-setup-plan.macos.sh"
      }
    },
    {
      "id": "show-haven-42-menu",
      "name": "Show Haven 42 Menu",
      "purpose": "Generate a short intent-based menu over stable workflows so users do not need to choose from individual scripts.",
      "category": "onboarding",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["platform", "outputPath", "markdownOutputPath"],
      "outputs": ["agentPackMenuJson", "agentPackMenuMarkdown"],
      "entryPoints": {
        "windows": "scripts/show-haven-42-menu.ps1",
        "linux": "scripts/show-haven-42-menu.linux.sh",
        "macos": "scripts/show-haven-42-menu.macos.sh"
      }
    },
    {
      "id": "show-workflow-chooser",
      "name": "Show Workflow Chooser",
      "purpose": "Generate a complete registry-backed workflow chooser with safety levels, commands, and reference docs.",
      "category": "onboarding",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["platform", "outputPath", "markdownOutputPath"],
      "outputs": ["workflowChooserJson", "workflowChooserMarkdown"],
      "entryPoints": {
        "windows": "scripts/show-workflow-chooser.ps1",
        "linux": "scripts/show-workflow-chooser.linux.sh",
        "macos": "scripts/show-workflow-chooser.macos.sh"
      }
    },
    {
      "id": "pull-local-agent-models",
      "name": "Pull Local Agent Models",
      "purpose": "Download selected local model candidates after hardware and validation review.",
      "category": "model-installation",
      "safetyLevel": "network-write",
      "uiReady": true,
      "inputs": ["models", "ollamaBaseUrl", "dryRun"],
      "outputs": ["pullSummaryJson"],
      "entryPoints": {
        "windows": "scripts/pull-local-agent-models.ps1",
        "linux": "scripts/pull-local-agent-models.linux.sh",
        "macos": "scripts/pull-local-agent-models.macos.sh"
      }
    },
    {
      "id": "test-local-agent-models",
      "name": "Test Local Agent Models",
      "purpose": "Run API-level model preflight checks, optional pulls, cleanup, and unload behavior against a local model server.",
      "category": "model-validation",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["models", "targetRepo", "ollamaBaseUrl", "modelProfilePath", "pullMissing", "unloadAfterEach", "removeFailedModels"],
      "outputs": ["localAgentModelTestReportJson"],
      "entryPoints": {
        "windows": "scripts/test-local-agent-models.ps1",
        "linux": "scripts/test-local-agent-models.linux.sh",
        "macos": "scripts/test-local-agent-models.macos.sh"
      }
    },
    {
      "id": "recommend-agent-config",
      "name": "Recommend Agent Config",
      "purpose": "Create hardware-aware model lanes and local configuration recommendations from sanitized profile and evidence data.",
      "category": "configuration",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["modelProfilePath", "modelCatalogPath", "evidenceCatalogPath", "outputPath"],
      "outputs": ["recommendedAgentConfigJson"],
      "entryPoints": {
        "windows": "scripts/recommend-local-agent-config.ps1",
        "linux": "scripts/recommend-local-agent-config.linux.sh",
        "macos": "scripts/recommend-local-agent-config.macos.sh"
      }
    },
    {
      "id": "recommend-language-model-lane",
      "name": "Recommend Language Model Lane",
      "purpose": "Classify a project and recommend only the model lane validated for its optional language rule pack and requested workflow operation.",
      "category": "configuration",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["targetRepo", "operation", "operatingSystem", "surface", "surfaceVersion", "provider", "outputPath"],
      "outputs": ["languageModelLaneRecommendationJson", "continueModelProfiles"],
      "entryPoints": {
        "windows": "scripts/recommend-language-model-lane.ps1",
        "linux": "scripts/recommend-language-model-lane.linux.sh",
        "macos": "scripts/recommend-language-model-lane.macos.sh"
      }
    },
    {
      "id": "apply-agent-config",
      "name": "Apply Agent Config",
      "purpose": "Write local-only agent configuration from a recommendation result with dry-run support.",
      "category": "configuration",
      "safetyLevel": "approved-write",
      "uiReady": true,
      "inputs": ["targetRepo", "recommendationPath", "ollamaBaseUrl", "dryRun"],
      "outputs": ["localConfigYaml"],
      "entryPoints": {
        "windows": "scripts/apply-recommended-agent-config.ps1",
        "linux": "scripts/apply-recommended-agent-config.linux.sh",
        "macos": "scripts/apply-recommended-agent-config.macos.sh"
      }
    },
    {
      "id": "setup-agent-surface",
      "name": "Set Up Agent Surface",
      "purpose": "Plan, install, configure, or health-check an evidence-backed non-Continue agent adapter, starting with Aider.",
      "category": "agent-surface-setup",
      "safetyLevel": "approved-write",
      "uiReady": true,
      "inputs": ["surface", "action", "targetRepo", "modelOrRecommendation", "lane", "ollamaBaseUrl", "installMethod", "dryRun"],
      "outputs": ["localSurfaceConfig", "healthResult", "nextCommand"],
      "entryPoints": {
        "windows": "scripts/setup-agent-surface.ps1",
        "linux": "scripts/setup-agent-surface.linux.sh",
        "macos": "scripts/setup-agent-surface.macos.sh"
      }
    },
    {
      "id": "classify-project",
      "name": "Classify Project",
      "purpose": "Inspect repository filenames without reading file contents, emit a sanitized project profile, and identify evidence-backed optional rule packs.",
      "category": "configuration",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["targetRepo", "outputPath"],
      "outputs": ["projectProfileJson", "selectedRulePackIds"],
      "entryPoints": {
        "windows": "scripts/get-project-profile.ps1",
        "linux": "scripts/get-project-profile.linux.sh",
        "macos": "scripts/get-project-profile.macos.sh"
      }
    },
    {
      "id": "install-pack-assets",
      "name": "Install Pack Assets",
      "purpose": "Install or update project-local or centralized agent assets with backups, dry-run, and global config support.",
      "category": "installation",
      "safetyLevel": "approved-write",
      "uiReady": true,
      "inputs": ["targetRepo", "sharedAssets", "globalConfigPath", "dryRun"],
      "outputs": ["installedAssets", "backupFolder", "globalConfigYaml"],
      "entryPoints": {
        "windows": "scripts/install-continue-pack.ps1",
        "linux": "scripts/install-continue-pack.linux.sh",
        "macos": "scripts/install-continue-pack.macos.sh"
      }
    },
    {
      "id": "generate-runtime-context",
      "name": "Generate Runtime Context",
      "purpose": "Create sanitized repository context for model-backed validation and review workflows.",
      "category": "validation",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["targetRepo", "outputPath"],
      "outputs": ["runtimeContextMarkdown"],
      "entryPoints": {
        "windows": "scripts/generate-runtime-context.ps1",
        "linux": "scripts/generate-runtime-context.linux.sh",
        "macos": "scripts/generate-runtime-context.macos.sh"
      }
    },
    {
      "id": "run-continue-with-runtime-policy",
      "name": "Run Continue With Runtime Policy",
      "purpose": "Run one Continue CLI prompt with resident-model limits and explicit unload enforcement when configured.",
      "category": "validation",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["model", "prompt", "configPath", "targetRepo", "ollamaBaseUrl", "continueCommand", "readOnly"],
      "outputs": ["continueCliOutput", "residentModelCleanup"],
      "entryPoints": {
        "windows": "scripts/run-continue-with-runtime-policy.ps1",
        "linux": "scripts/run-continue-with-runtime-policy.linux.sh",
        "macos": "scripts/run-continue-with-runtime-policy.macos.sh"
      }
    },
    {
      "id": "run-runtime-validation",
      "name": "Run Runtime Validation",
      "purpose": "Execute configured validation prompts, collect raw local outputs, and optionally append sanitized summaries.",
      "category": "validation",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["targetRepo", "configPath", "appendSummary"],
      "outputs": ["runtimeValidationOutputFolder", "runtimeValidationSummary"],
      "entryPoints": {
        "windows": "scripts/run-runtime-validation.ps1",
        "linux": "scripts/run-runtime-validation.linux.sh",
        "macos": "scripts/run-runtime-validation.macos.sh"
      }
    },
    {
      "id": "verify-runtime-output",
      "name": "Verify Runtime Output",
      "purpose": "Check runtime validation output for invented filenames, unsupported claims, and other deterministic failure signals.",
      "category": "validation",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["outputFolder", "targetRepo"],
      "outputs": ["runtimeVerificationReport"],
      "entryPoints": {
        "windows": "scripts/verify-runtime-output.ps1",
        "linux": "scripts/verify-runtime-output.linux.sh",
        "macos": "scripts/verify-runtime-output.macos.sh"
      }
    },
    {
      "id": "test-local-agent-health",
      "name": "Test Local Agent Health",
      "purpose": "Check local setup health for repository config, runtime output, and optional Ollama reachability without mutating user files.",
      "category": "health",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["targetRepo", "ollamaBaseUrl", "skipOllama", "outputPath"],
      "outputs": ["localAgentHealthReportJson", "healthConsoleOutput"],
      "entryPoints": {
        "windows": "scripts/test-local-agent-health.ps1",
        "linux": "scripts/test-local-agent-health.linux.sh",
        "macos": "scripts/test-local-agent-health.macos.sh"
      }
    },
    {
      "id": "cleanup-local-agent-artifacts",
      "name": "Cleanup Local Agent Artifacts",
      "purpose": "Safely remove ignored local validation output, generated samples, backup folders, and failed diagnostic artifacts after dry-run review.",
      "category": "maintenance",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["targetRepo", "outputPath", "apply", "includeRuntimeOutput", "includeGeneratedSamples", "includeBackups", "includeFailedReports"],
      "outputs": ["cleanupPlanJson", "removedLocalArtifacts"],
      "entryPoints": {
        "windows": "scripts/cleanup-local-agent-artifacts.ps1",
        "linux": "scripts/cleanup-local-agent-artifacts.linux.sh",
        "macos": "scripts/cleanup-local-agent-artifacts.macos.sh"
      }
    },
    {
      "id": "test-agent-cli-surface",
      "name": "Test Agent CLI Surface",
      "purpose": "Screen CLI-capable agent surfaces with read-only and disposable write-smoke checks through the shared harness.",
      "category": "agent-surface-validation",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["surfaceName", "surfaceKey", "agentCommand", "models", "targetRepo", "dryRun", "includeWriteSmoke"],
      "outputs": ["agentCliSurfaceTestReportJson"],
      "entryPoints": {
        "windows": "scripts/test-agent-cli-surface-models.ps1",
        "linux": "scripts/test-agent-cli-surface-models.linux.sh",
        "macos": "scripts/test-agent-cli-surface-models.macos.sh"
      }
    },
    {
      "id": "generate-sample-repositories",
      "name": "Generate Sample Repositories",
      "purpose": "Create deterministic disposable sample repositories for validation without private source code.",
      "category": "sample-generation",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["outputRoot", "force", "list"],
      "outputs": ["sampleRepositories"],
      "entryPoints": {
        "windows": "scripts/generate-sample-repositories.ps1",
        "linux": "scripts/generate-sample-repositories.linux.sh",
        "macos": "scripts/generate-sample-repositories.macos.sh"
      }
    },
    {
      "id": "validate-pack",
      "name": "Validate Pack",
      "purpose": "Run static pack validation for config, docs, scripts, references, and safety invariants.",
      "category": "release-readiness",
      "safetyLevel": "read-only",
      "uiReady": true,
      "inputs": ["expectedVersion"],
      "outputs": ["validationConsoleOutput"],
      "entryPoints": {
        "windows": "scripts/validate-pack.ps1",
        "linux": "scripts/validate-pack.linux.sh",
        "macos": "scripts/validate-pack.macos.sh"
      }
    },
    {
      "id": "test-pack",
      "name": "Test Pack",
      "purpose": "Run the repository test suite used before commits, pushes, and releases.",
      "category": "release-readiness",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["expectedVersion"],
      "outputs": ["testConsoleOutput"],
      "entryPoints": {
        "windows": "scripts/test-pack.ps1",
        "linux": "scripts/test-pack.linux.sh",
        "macos": "scripts/test-pack.macos.sh"
      }
    },
    {
      "id": "test-release-readiness",
      "name": "Test Release Readiness",
      "purpose": "Run the release gate for validation, tests, release package dry-run, git state, workflow registry, and agent-surface parity.",
      "category": "release-readiness",
      "safetyLevel": "controlled-write",
      "uiReady": true,
      "inputs": ["expectedVersion", "releaseVersion", "outputPath", "allowDirty", "skipValidation", "skipTests", "skipPackageDryRun"],
      "outputs": ["releaseReadinessReportJson", "releaseReadinessConsoleOutput"],
      "entryPoints": {
        "windows": "scripts/test-release-readiness.ps1",
        "linux": "scripts/test-release-readiness.linux.sh",
        "macos": "scripts/test-release-readiness.macos.sh"
      }
    },
    {
      "id": "verify-hosted-ci",
      "name": "Verify Hosted CI",
      "purpose": "Verify the exact pushed commit through GitHub Actions, wait for completion, check required cross-platform jobs, and retrieve failed logs.",
      "category": "release-readiness",
      "safetyLevel": "network-read",
      "uiReady": true,
      "inputs": ["repository", "commitSha", "workflow", "runId", "discoveryTimeoutSeconds"],
      "outputs": ["pushState", "commitSha", "runUrl", "requiredJobResults"],
      "entryPoints": {
        "windows": "scripts/verify-hosted-ci.ps1",
        "linux": "scripts/verify-hosted-ci.linux.sh",
        "macos": "scripts/verify-hosted-ci.macos.sh"
      }
    },
    {
      "id": "build-release-package",
      "name": "Build Release Package",
      "purpose": "Create release archives, checksums, and release artifacts after validation passes.",
      "category": "release-readiness",
      "safetyLevel": "controlled-write",
      "uiReady": false,
      "inputs": ["version", "outputRoot", "dryRun"],
      "outputs": ["releaseArchive", "checksumFile"],
      "entryPoints": {
        "windows": "scripts/build-release-package.ps1",
        "linux": "scripts/build-release-package.linux.sh",
        "macos": "scripts/build-release-package.macos.sh"
      }
    }
  ]
}
